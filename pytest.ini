[pytest]
markers =
    ci: fast, focused tests for continuous integration
    integration: tests that require running services (API/WS/Redis)
    perf: performance/throughput tests
addopts = -m "not integration"
